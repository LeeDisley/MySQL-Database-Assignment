SELECT Nok_ID, Nok_Name
FROM NextOfKin;

SELECT Course_ID, Course_Title
FROM Courses;

SELECT Payment_Code, Payment_Descr
FROM PaymentMethods;

SELECT Details_Code, Details_Student_ID
FROM StudentPaymentDetails;

SELECT Invoice_Num, Invoice_Student_ID
FROM Invoice;

SELECT Stud_Address_ID, Stud_Add_Date, Stud_Student_ID
FROM StudentAddress;

SELECT Lease_Num, Lease_Duration
FROM Lease;

SELECT Inspect_ID, Inspect_Staff_ID, Inspect_Date
FROM Flat_Inspections;

SELECT Staff_ID, Staff_Name
FROM Staff;

SELECT Uni_Accom_ID, Uni_Accom_Building_Name
FROM UniAccomodation;

SELECT Add_ID, Add_Street
FROM Address;

SELECT Place_Num, Room_Number
FROM Room;

SELECT Student_ID, Sudent_First_Name, Student_Second_Name
FROM Student;

SELECT Home_Add_ID, Home_Add_City, Home_Add_PCode
FROM HomeAddress;
